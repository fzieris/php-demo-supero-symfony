{% extends "admin/admin-base.html.twig" %}

{% import "admin/_buttons.html.twig" as button %}

{% block body %}
    <h3>Helden bearbeiten</h3>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path("admin-list-heros") }}" class="btn btn-default">Zurück</a>
    <button type="submit" class="btn btn-primary">
        <i class="fas fa-check"></i>
        Speichern
    </button>
    {{ button.delete_hero(form.vars.data, 'btn-sm pull-right') }}
    {{ form_end(form) }}
{% endblock %}
